{
  "name": "gcp_list_billing_services",
  "description": "List GCP billing services (Google Cloud services that can incur charges)",
  "destructive": false,
  "inputSchema": {
    "type": "object",
    "properties": {
      "page_size": {
        "type": "number",
        "description": "Number of services to return (default: 20, max: 5000)"
      }
    },
    "required": []
  }
}
